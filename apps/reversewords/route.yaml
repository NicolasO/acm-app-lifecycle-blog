apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    openshift.io/host.generated: "true"
  name: reverse-words
spec:
  port:
    targetPort: http
  to:
    kind: Service
    name: reverse-words
    weight: 100
  wildcardPolicy: None

